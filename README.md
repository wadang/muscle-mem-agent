# Lenovo TSP Muscle Memory Agent

## 安装

推荐使用 `uv`（项目已包含 `uv.lock`）：

```bash
uv sync
```

如果你希望使用 `pip`（可编辑安装）：

```bash
python -m venv .venv
source .venv/bin/activate  # Windows: .venv\Scripts\activate
pip install -e .
```

